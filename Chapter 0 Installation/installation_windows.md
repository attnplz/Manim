# Installation Manim on Windows

To install the Manim Community Edition using the Windows command line, the recommended approach is to use Python's package manager, pip, within a virtual environment. This requires you to have Python (version 3.13 or earlier) and FFmpeg already installed and added to your system's PATH. 

## Prerequisite
Before installing Manim, ensure you have the following system libraries:

